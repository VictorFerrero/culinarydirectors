  //TODO: abstract doGET and doPOST to global class
  //TODO: create new function as wrapper, i.e. doReq
  /*$scope.doGET = function(uri, callback) {
	$http.get('../../index.php/'+uri).
	  then(function(response) {
		
	  }, function(response) {
		
	  });
  }
  $scope.doPOST = function(uri, dataObj, callback) {
	$http.post('../../index.php/'+uri, dataObj).
	  then(function(response) {
		
	  }, function(response) {
		
	  });
  }
  $scope.l = function(tag, toLog){console.log(tag.toUpperCase() + ": "+ toLog);}
  */